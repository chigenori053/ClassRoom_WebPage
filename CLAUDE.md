# KuKKA Webページリニューアルプロジェクト
Version: 1.1
最終更新: 2026-03-03

---

## 1. プロジェクト概要

**教室名：** KuKKA（プログラミング教室）

**目的：** 機能・情報の羅列による集客ではなく、**教室の理念・教育思想を深く理解してもらうための「思想表明ページ（ブランドサイト）」** を構築する。マス集客モデルではなく「**共感選別モデル**（純度・濃度を追う戦略）」を採用。

**技術スタック：**
- Framework: Next.js (App Router, v16.1.5)
- UI Library: React (v19.2.3)
- 言語: TypeScript
- スタイリング: **CSS Modules** (`globals.css`, `*.module.css`)
- タイポグラフィ: `next/font/google`（Noto Serif JP 等の明朝体を活用）
- デプロイ: Vercel（想定）

**進め方：** オーナー1名 ＋ Claude（実装・設計・コピー全般を担当）

---

## 2. Claudeの役割と優先順位

このプロジェクトでClaudeは以下の4つの役割を担う。
タスクの種類に応じて、対応するモードで動作する。

| モード | 役割 | 主なアウトプット |
|---|---|---|
| **Copywriter** | コピーライティング・文章作成 | LP文章、コース説明、ブログ記事 |
| **Designer** | デザイン提案・UI設計 | コンポーネント設計、レイアウト案、CSSスタイル |
| **Coder** | コーディング実装 | Next.jsコンポーネント、ページ実装 |
| **Reviewer** | コードレビュー・品質チェック | 指摘リスト、改善提案 |

> 指示が曖昧な場合は、どのモードで対応するかをまず確認してから作業を開始する。

---

## 3. サイト構成（実装ページ）

**公開サイト（リポジトリルート、Next.js App Router / ポート3000）：**

```
/                    # トップ（思想表明・ヒーロー・コース紹介・CTA）
/courses             # コース詳細（Sprout / Grow / Bloom）
/pricing             # 料金とシステム
/faq                 # よくある質問
/access              # 教室へのアクセス
/booking             # 体験レッスン予約フォーム
/column              # コラム（自前CMS。管理画面で執筆・公開した記事をPostgreSQL経由で表示）
/contact             # お問い合わせ（掲載日程外の体験教室希望など）
```

**管理画面（`admin/` 配下・別Next.jsアプリ / ポート3001）：**

管理画面は公開サイトとは別デプロイの独立したNext.jsプロジェクトとして `admin/` に実装されている（`npm run dev:admin` で起動）。公開サイトと同じPostgreSQLデータベースを共有し、Cookie（`admin_auth`）によるログイン保護がかかっている。

```
/                    # ログイン
/dashboard           # ダッシュボード（未確認の予約・問い合わせ件数など）
/bookings            # 体験レッスン予約管理
/schedules           # 授業スケジュール管理（Google Apps Script経由でGoogleカレンダーと連携）
/calendar            # カレンダー表示
/inquiries           # スケジュール外お問い合わせ管理
/columns             # コラム記事の執筆・公開管理（Markdown）
```

各ページを実装・編集する際は、このサイト構成を常に参照する。管理画面に手を入れる際は対象が `admin/` 配下であることを確認する。

---

## 4. コンセプトとコアメッセージ

**メインコピー：** 「Think. And enjoy it. 考えることが、好きになる場所。」

**サブコピー：** 「AIがある時代だからこそ、自分の頭で考えられる子に育ってほしい。」

**教育スタンス：** 学習初期はあえてAIを使わない。エラーを自分で考え、試行錯誤を繰り返す中に本当の力が育つ。土台ができてから段階的にAIを導入する「AIとの向き合い方」を学ぶ教育。単なる「AI禁止」ではなく「自走力育成のための戦略的制限」。

**サイトの在り方：** 情報サイトではなく「**思想表明ページ**」。装飾を削ぎ落とし、言葉と余白の力で理念を伝える。

---

## 5. ターゲットユーザーと文章トーン

**メインターゲット：**
- 小学生〜中学生の保護者（30〜40代）
- **深層心理：** AIの急速な普及に対し「このままで子どもの将来は大丈夫か」「考える力が失われないか」という本質的な不安を抱えている
- 単なる「ITスキルの先取り」ではなく、「**これからの時代を生き抜くための本質的な教育**」を探している層

**文章トーンの原則：**
- **温かく・親しみやすく**（思想表明型でも「冷たさ」を生まない）
- **具体的に・わかりやすく**（専門用語は必ず補足）
- **不安に寄り添い、背中を押す**（「楽しく、丁寧に、一人ひとりに合わせて。」など安心ワンフレーズを意識）
- **言葉の密度を上げる**（情報量より密度。長文化・重篤化を避ける）

> コピーを書く際は必ず上記トーンに沿っているかを自己チェックしてから提出する。

---

## 6. コーディング規約

### 基本方針
- コンポーネントは **機能単位で小さく分割** する
- `app/` ディレクトリ構成（Next.js App Router）に従う
- スタイルは **CSS Modules のみ** 使用（`*.module.css`）。インラインstyle禁止
- 画像は必ず `next/image` を使用する
- リンクは必ず `next/link` を使用する

### ファイル命名規則
```
components/     # 再利用コンポーネント（PascalCase: HeroSection.tsx）
app/            # ページ・レイアウト（kebab-case: /courses/page.tsx）
lib/            # ユーティリティ関数（camelCase: formatDate.ts）
```

### 型定義
- `any` の使用は原則禁止
- Props には必ず型定義を付ける
- 外部データ（フォーム・API）は `zod` でバリデーションする（推奨）

### アクセシビリティ
- 画像には必ず `alt` を付ける
- インタラクティブ要素には `aria-label` を付ける
- カラーコントラスト比は WCAG AA 準拠を目指す

---

## 7. コードレビューのチェックリスト

Reviewerモードで動作する際は、以下を必ず確認して報告する。

**品質チェック：**
- [ ] TypeScriptの型エラーがないか
- [ ] `any` が使われていないか
- [ ] 未使用のimport・変数がないか
- [ ] `console.log` が残っていないか

**Next.js固有：**
- [ ] `use client` の使用が最小限か（不要なClient Componentになっていないか）
- [ ] `next/image` / `next/link` を正しく使っているか
- [ ] metadataの設定が漏れていないか

**UI/アクセシビリティ：**
- [ ] モバイル表示が崩れていないか（レスポンシブ確認）
- [ ] alt / aria-label が設定されているか

**報告フォーマット：**
```
## レビュー結果
対象ファイル: xxx.tsx
判定: ✅ PASS / ⚠️ WARN / ❌ FAIL

### 指摘事項
1. [重要度: high/medium/low] 内容 → 改善案

### 良かった点
- ...
```

---

## 8. デザイン方針

### ビジュアルの方向性（Organic & Minimal）
- **完全な無機質（純白・グレー）を避け**、オフホワイトや自然由来のアースカラー（くすんだ緑・テラコッタ等）をベースにした温かみのある色調
- 言葉の密度と **余白（ホワイトスペース）** を活かした静謐なデザイン
- 子どもの真剣な表情・タイピングする小さな手・講師が見守る写真など、**「実在感」のある写真**で人間味を補完

### カラーパレット
```
Base:      オフホワイト系（清潔感 + 温かみ）
Accent:    アースカラー（くすんだ緑・テラコッタ等）
Text:      深みのあるダークトーン（読みやすさ）
```
> オーナーの指示でカラーパレットが変更された場合は、このセクションを更新する。

### タイポグラフィ
- `next/font/google` で最適化
- 明朝体（Noto Serif JP 等）を効果的に用い、言葉の重みと知性を強調

### CTAの考え方（重要）
- **強いCTA（ポップアップ・追従ボタン等）は配置しない**
- 体験教室への導線は、理念を読み終えた最下部に「**共感の果ての、招待**」として静かに配置する
- コンバージョンのための強引な誘導ではなく、共感した人への上質な案内として設計する

### コンポーネント設計の優先順位
1. モバイルファースト
2. 読み込み速度（画像最適化・遅延ロード）
3. 余白と言葉の密度（情報の詰め込み禁止）

---

## 9. 禁止事項

以下はClaudeが自律的に行ってはならない：

- **個人情報・実際の生徒名・写真を文章内に使用すること**
- **外部サービスのAPI Key・認証情報をコードに直書きすること**（必ず `.env.local` に）
- **デザイン・コンポーネント構成の大幅変更を事前確認なしに行うこと**
- **`/booking` `/contact` フォームのバリデーション・送信処理をテストなしに本番適用すること**
- **強いCTA・追従ボタン・ポップアップを追加すること**（デザイン方針に反する）

---

## 10. 作業開始時のルーティン

タスクを受け取ったら、必ず以下の順で確認してから作業する：

```
1. タスクがどのモード（Copywriter / Designer / Coder / Reviewer）か判断する
2. 対象ページ・コンポーネントをサイト構成（Section 3）で確認する
3. コーディングタスクの場合、コーディング規約（Section 6）を参照する
4. 文章タスクの場合、コンセプト（Section 4）とターゲット・トーン（Section 5）を確認する
5. 完了後、Reviewerチェックリスト（Section 7）で自己レビューを行う
```

---

## 11. 応答ルール

- **日本語で応答する**
- コード・ファイルパス・技術用語は英語のまま使用する
- 提案が複数ある場合は **A案／B案** の形で選択肢を示す
- 実装前に **「何を作るか」を1〜2行で確認してから** コードを書く
- 不明点があれば実装を止めて質問する（推測で進めない）
